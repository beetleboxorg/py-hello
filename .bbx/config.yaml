runners:
 local-runner:
  image: stag:5000/ubuntu-generic:latest
  
jobs:   
 build_run_sim:
  resource_spec: micro 
  runner: local-runner
  steps:
  - run:
      name: Setup Environment
      command: |
       echo $HELLO_WORLD
      env:
       HELLO_WOLD : hello world
       foo : bar
    
workflows:
 complete-build-test:
  jobs:
   - build_run_sim
runners:
 AWS-runner:
  image: ubuntu-generic

jobs:   
 hello-world:
  runner: AWS-runner
  output:
   artifact:
    - hello.txt  
  steps:
   - run:
      name: Say Hello World
      command: echo "Hello World" > hello.txt
      
workflows:
 hello-world-workflow:
  jobs:
   - hello-world

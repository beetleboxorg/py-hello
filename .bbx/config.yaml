runners:
 AWS-runner:
  image: ip-192-168-102-216:5000/ubuntu-generic:latest
jobs:   
 hello-world:
  resource_spec: micro
  runner: AWS-runner
  current_working_directory: ~/
  output:
   artifact:
    - hello.txt  
  steps:
   - run:
      name: Ping
      command: |
       apt-get update -y
       apt-get install iputils-ping -y
   - run:
      name: Say Hello World
      command: echo "Hello World" 
   - run:
      name: Text Hello World
      command: echo "Hello World" > hello.txt    
   - run:
      name: fail
      command: echo "cheese"

      
workflows:
 hello-world-workflow:
  triggers:
   - manual 
   - schedule:
      cron: "45 15 * * *"    
  jobs:
   - hello-world
   
